/*const Discord = require('discord.js');
const execute = (bot, msg, args) => { 


var list = [
  'https://i.imgur.com/gn9MyP6.jpeg',
  'https://i.imgur.com/gn9MyP6.jpeg',
  'https://i.imgur.com/gn9MyP6.jpeg'
];

var rand = list[Math.floor(Math.random() * list.length)];
let user = msg.mentions.users.first() || bot.users.cache.get(args[0]);
if (!user) {
return msg.reply('lembre-se de mencionar um usuário válido para comer!');
}
/*
message.channel.send(`${message.author.username} **acaba de beijar** ${user.username}! :heart:`, {files: [rand]});

let avatar = msg.author.displayAvatarURL({format: 'png'});
  const embed = new Discord.MessageEmbed()
        
        .setColor('#000000')
        .setDescription(`${msg.author} acaba de comer o cu ${user} que cu gostoso!`)
        .setImage(rand)
        .setTimestamp()
        .setThumbnail(avatar)
        .setFooter('Cuzão gostoso rapaz')
        .setAuthor(msg.author.tag, avatar);
 msg.channel.send(embed);

}
module.exports = {
    name: "comer",
  
    execute,
}
*/